#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 *  @c: return char type
 *
 *  Return: 1 for fail and 0 for success
 */

int_isupper(int c)
{

	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	return (0);
}
