#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 *  @x: the number to be checked
 *
 *  Return: 1 for uppercase, 0 for other else
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
